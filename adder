module fulladder( 
    input a, b, cin,
    output cout, sum );
	assign sum = a^b^cin;
    assign cout = (a&b) | (a&cin) | (b&cin);
endmodule

module adder3bits(
    input [2:0] a, b,
    input cin,
    output cout,
    output[2:0] sum);

    assign sum = 
    endmodule
